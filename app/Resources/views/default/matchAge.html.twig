{% extends '::base.html.twig' %}

{% block body %}
    <h3>Matches</h3>
    <div class="match_tables">
        <table style="float: left">
            <tr>
                <th>Matched Employee</th>
                <th>Age</th>
            </tr>
            {% for employee in employees %}
                <tr>
                    <td>{{ employee.firstname }} {{ employee.lastname }}</td>
                    <td>{{ employee.age }}</td>
                </tr>
            {% endfor %}
        </table>
        <table style="float: left">
            <tr>
                <th>Newbie</th>
                <th>Age</th>
            </tr>
            {% for newbie in newbies %}
                <tr>
                    <td>{{ newbie.firstname }} {{ newbie.lastname }}</td>
                    <td>{{ newbie.age }}</td>
                </tr>
            {% endfor %}
        </table>
        <form action="{{ render(controller("AppBundle:Default:filterByNationality")) }}">
            <button type="submit" class="btn btn-lg btn-success" >Match</button>
        </form>
    </div>
{% endblock %}