{% extends '::base.html.twig' %}

{% block body %}
    <h3>Matches</h3>
    <div class="match_tables">
        <table style="float: left">
            <tr>
                <th>Matched Employee</th>
                <th>Nationality</th>
            </tr>
            {% for employee in employees %}
                <tr>
                    <td>{{ employee.firstname }} {{ employee.lastname }}</td>
                    <td>{{ employee.nationality }}</td>
                </tr>
            {% endfor %}
        </table>
        <table style="float: left">
            <tr>
                <th>Newbie</th>
                <th>Nationality</th>
            </tr>
            {% for newbie in newbies %}
                <tr>
                    <td>{{ newbie.firstname }} {{ newbie.lastname }}</td>
                    <td>{{ newbie.nationality }}</td>
                </tr>
            {% endfor %}
        </table>
    </div>
    <button onclick="{{ render(controller("AppBundle:Default:filterByNationality")) }}">Click me</button>
{% endblock %}